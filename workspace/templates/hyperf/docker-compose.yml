services:
  app:
    image: $WORKSPACE_NAME/hyperf:latest
    build:
      context: $TPL_PATH
      args:
        - USER_ID=$USER_ID
        - GROUP_ID=$GROUP_ID
    hostname: "$APP_NAME.$BASE_DOMAIN"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      VIRTUAL_HOST: "$APP_NAME.$BASE_DOMAIN"
      VIRTUAL_PORT: "9501"
      HOME: /tmp/home
      COMPOSER_HOME: /tmp/home/composer
      COMPOSER_CACHE_DIR: /tmp/home/composer_cache
    working_dir: /var/www
    entrypoint:
      - "/usr/local/bin/entrypoint"
    volumes:
      - "$HOME_PATH:/tmp/home"
      - "$HOME_PATH/.config:/.config"
      - "$TPL_PATH/entrypoint:/usr/local/bin/entrypoint"
      - "$SVC_PATH:/var/www"
    networks:
      - dev

networks:
  dev:
    external: true
    name: $NETWORK